const TodoFilters = () => {
  return (
    <div>
      <span>Show:</span>
      <button>All</button>
      <button>Completed</button>
      <button>Incompleted</button>
    </div>
  );
};

export default TodoFilters;
